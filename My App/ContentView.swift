import SwiftUI

struct ContentView: View {
    var body: some View {
        Form {
            Button {
                //DispatchQueue.global(qos: .utility).async {
                    //Stuff
                //}
            } label: {
                Text("Trust")
            }
        }
    }
}
